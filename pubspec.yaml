name: jetx
description: A modern Flutter framework for state management, dependency injection, and route management. An actively maintained fork of GetX.
version: 1.0.0
homepage: https://github.com/alamre/jetx

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
