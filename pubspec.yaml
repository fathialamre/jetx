name: jet
description: A powerful Flutter framework for state management, routing, and dependency injection - forked from GetX
version: 1.0.0
homepage: https://github.com/alamre/jet

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: ">=1.0.0 <2.0.0"

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
