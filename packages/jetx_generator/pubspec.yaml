name: jetx_generator
description: Code generator for JetX routes
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  jetx_annotations:
    path: ../jetx_annotations
  build: ^2.4.0
  build_config: ^1.1.0
  source_gen: ^1.5.0
  analyzer: ^6.0.0
  code_builder: ^4.10.0
  dart_style: ^2.3.0

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^2.2.0
  test: ^1.25.0

